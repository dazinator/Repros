<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="Todo.TodoListPage" Title="Todo">
	<ContentPage.ToolbarItems>
		<ToolbarItem Text="+" Clicked="OnItemAdded">
			<ToolbarItem.Icon>
				<OnPlatform x:TypeArguments="FileImageSource" Android="plus.png" />
			</ToolbarItem.Icon>
		</ToolbarItem>
	</ContentPage.ToolbarItems>
	<ListView x:Name="listView" Margin="20" ItemTapped="OnListItemSelected">
		<ListView.ItemTemplate>
			<DataTemplate>
				<ViewCell>
					<StackLayout Margin="20,0,0,0" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
						<Label Text="{Binding Name}" VerticalTextAlignment="Center" HorizontalOptions="StartAndExpand" />
						<Image Source="check.png" HorizontalOptions="End" IsVisible="{Binding Done}" />
					</StackLayout>
				</ViewCell>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
    <StackLayout>
        <Button Text="Add Account" Clicked="OnAddAccount" />
        <Button Text="Select Account" Clicked="OnSelectAccount" />
        <Button Text="Get Token" Clicked="OnGetToken" />
        <Label x:Name="tokenLabel" Text="" />
    </StackLayout>
</ContentPage>